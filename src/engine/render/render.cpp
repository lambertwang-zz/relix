#include "render.h"

// Library
#include <stdlib.h>
#include <math.h>
#include <string.h>

// Engine
#include "constants.h"

#ifndef max
#define max(a, b) ((a) > (b) ? (a) : (b))
#endif

#ifndef min 
#define min(a, b) ((a) < (b) ? (a) : (b))
#endif

namespace rlx {

}
